<div class="row">
  <h4 class="center-align"><%= @project.title %></h4>
  <h6 class="center-align"><%= @project.user.email %></h6>
</div>
<div class="row">
  
  <div class="col s8">
    <%= image_tag(@project.image.url, width: "100%", alt: "Imagem") %>
    <div class="divider"></div>
    <div class="section">
      <%= @project.descripton %>
    </div>
  </div>

  <div class="col s4">
    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <span class="card-title">R$ <%= @project.amount %></span>
        <p>Você pode ser um apoiador, junte-se a nós. ;-)</p>
        <p>
          Lorem ipsum dolor sit amet, consectetur adipiscing elit.
          Nunc at lectus malesuada, dapibus est at, elementum magna.
          Suspendisse tempor leo et urna tincidunt viverra
        </p>
        <br>
        <p>
          Meta:<span class="green-text"> <b>R$ <%= @project.goal %></b></span>
        </p>
      </div>
      <div class="card-action">
        <a href="<%= "/donate/#{@project.id}" %>">Apoiar</a>
        <a href="<%= root_path %>">Mais projetos</a>
      </div>
    </div>
    Listagem de doações
    <div class="collection">
      <% @projects.donate.each do |donate| %>
        <tr>
          <td><%= project.title %></td>
          <td>R$ <%= project.goal %></td>
          <td>R$ <%= project.amount %></td>
          <td colspan="3">
            <%= link_to 'Visualizar', "/project/#{project.id}", class: "btn waves-effect waves-light" %>
            <%= link_to 'Editar', edit_project_path(project), class: "btn waves-effect waves-light" %>
            <%= link_to 'Remover', project, method: :delete, data: { confirm: 'Confirma a ação?' }, class: "btn waves-effect waves-light" %>
          </td>
        </tr>
        <a href="#!" class="collection-item"><span class="badge">1</span>Alan</a>
      <a href="#!" class="collection-item"><span class="new badge">4</span>Alan</a>
      <a href="#!" class="collection-item">Alan</a>
      <a href="#!" class="collection-item"><span class="badge">14</span>Alan</a>
      <% end %>
    </div>
  </div>
</div>